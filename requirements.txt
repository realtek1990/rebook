# ─── ReBook Core (cross-platform, ~100 MB) ───
markdown
EbookLib
beautifulsoup4
markdownify
litellm
PyMuPDF

# ─── macOS only (installed automatically) ───
# pyobjc-framework-Cocoa

# ─── Marker OCR (optional, ~1 GB) ───
# marker-pdf
